function sum(a, b) {
	return a + b;
}


function minus(a, b) {
	return a - b;
}

const letterArr = ['a', 'bb', 'c', 'd']
console.log(letterArr.join('.'));

module.exports = {
	sum,
	minus,
}
